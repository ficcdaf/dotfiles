-- local tv = require("taskview")

-- tv.setup({})
return {
  dir = "~/dev/taskview.nvim/",
  name = "taskview",
  lazy = false,
  -- opts = {},
  -- config = function()
  --   require("taskview")
  -- end,
  -- opts = {
  --   out = "Hi testing",
  -- }
}
